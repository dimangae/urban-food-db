ALTER TABLE PRODUCT
MODIFY (IMAGE_URL VARCHAR2(500));

ALTER TABLE USERS
ADD ID NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY;

ALTER TABLE USERS DROP CONSTRAINT USERS_PK;

ALTER TABLE USERS
ADD CONSTRAINT username_unique UNIQUE (username);

DROP TABLE USERS
